# Relative paths in Solidity's test/libyul/yulSyntaxTests where our parser's
# accept/reject result currently differs from the expectation embedded after
# `// ----`. Keep this sorted; CI reports unexpected and stale entries.
